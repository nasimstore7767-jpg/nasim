# دليل تركيب نظام Nasim ERP على اللابتوب (تشغيل محلي)

نظام **Nasim ERP** — نظام محاسبة وإدارة لمحل عالم البرمجة للصيانة والبرمجة.
النظام يعمل بالكامل في المتصفح (Offline-First) ويخزّن البيانات محلياً في المتصفح
عبر IndexedDB، فلا يحتاج إلى خادم قاعدة بيانات أو إنترنت بعد التركيب.

---

## ١) المتطلبات الأساسية

قبل البدء، ثبّت البرنامج التالي على اللابتوب:

### Node.js (إلزامي)
- حمّل النسخة LTS من: https://nodejs.org
- اختر النسخة **20 أو أحدث**.
- بعد التركيب، تأكد من نجاحه بفتح موجّه الأوامر (CMD / Terminal) وكتابة:
  ```
  node -v
  npm -v
  ```
  يجب أن يظهر رقم النسخة لكل أمر.

> ملاحظة: لا تحتاج إلى تركيب أي قاعدة بيانات (MySQL/SQL) — النظام يخزّن
> بياناته داخل المتصفح تلقائياً.

---

## ٢) فك ضغط الملفات

1. فك ضغط الملف `nasim-erp.zip` في أي مجلد (مثلاً سطح المكتب).
2. سيظهر مجلد باسم `webapp` بداخله ملفات المشروع.

---

## ٣) تركيب الحزم (مرة واحدة فقط)

1. افتح موجّه الأوامر **داخل مجلد المشروع** `webapp`:
   - **ويندوز:** افتح المجلد، اكتب `cmd` في شريط العنوان واضغط Enter.
   - **ماك/لينكس:** افتح Terminal ثم `cd` إلى مسار المجلد.
2. نفّذ الأمر التالي (يستغرق دقيقة إلى دقيقتين حسب سرعة الإنترنت):
   ```
   npm install
   ```
   هذا الأمر يحتاج إنترنت **مرة واحدة فقط** لتنزيل المكتبات.

---

## ٤) تشغيل النظام

### الطريقة (أ) — وضع الاستخدام اليومي (موصى به)
```
npm run build
npm run preview
```
بعد ظهور الرابط المحلي، افتح في المتصفح:
**http://localhost:3000**

### الطريقة (ب) — وضع التطوير (لو تريد تعديل الكود)
```
npm run dev
```
سيظهر رابط مثل `http://localhost:5173` — افتحه في المتصفح.

> أي طريقة تكفي للاستخدام. الطريقة (أ) أسرع وأخف للاستخدام اليومي.

---

## ٥) تسجيل الدخول لأول مرة

- **اسم المستخدم:** `nasim`
- **كلمة المرور:** `n123`

> يمكنك بعد الدخول إنشاء مستخدمين جدد وتغيير كلمة المرور من شاشة **المستخدمين**،
> وتعديل اسم المحل والشعار من شاشة **الإعدادات**.

---

## ٦) ملاحظات مهمة جداً عن البيانات

- البيانات تُحفظ **داخل متصفح اللابتوب نفسه** (IndexedDB).
- ⚠️ استخدم **نفس المتصفح ونفس المستخدم على ويندوز** دائماً، لأن تغيير
  المتصفح = قاعدة بيانات فارغة جديدة.
- ⚠️ **لا تستخدم وضع التصفّح الخفي (Incognito)** — البيانات تُمسح عند الإغلاق.
- لا تضغط "مسح بيانات الموقع / Clear browsing data" وإلا ستُفقد البيانات.
- يُنصح بأخذ نسخة احتياطية دورية من بيانات النظام (إن توفّرت خاصية التصدير).

---

## ٧) إنشاء اختصار سريع للتشغيل (اختياري — ويندوز)

لتشغيل النظام بنقرة واحدة دون كتابة أوامر:

1. أنشئ ملفاً نصياً جديداً داخل مجلد `webapp` باسم `تشغيل-النظام.bat`.
2. ضع بداخله:
   ```bat
   @echo off
   cd /d "%~dp0"
   call npm run preview
   pause
   ```
3. احفظه. الآن نقرة مزدوجة على `تشغيل-النظام.bat` تشغّل النظام مباشرة،
   ثم افتح الرابط الظاهر في المتصفح.

> (يجب أن تكون قد نفّذت `npm install` و `npm run build` مرة واحدة من قبل.)

---

## ٨) حل المشكلات الشائعة

| المشكلة | الحل |
|---|---|
| `npm: command not found` | لم يُركّب Node.js بشكل صحيح — أعد تركيبه وأعد فتح الموجّه. |
| الصفحة بيضاء بعد التشغيل | تأكد أنك فتحت الرابط الصحيح الظاهر في الموجّه (وليس رابطاً آخر). |
| البيانات اختفت | غالباً غيّرت المتصفح أو مسحت بيانات الموقع — استخدم نفس المتصفح دائماً. |
| `npm install` يفشل | تأكد من اتصال الإنترنت ثم أعد المحاولة، أو نفّذ `npm cache clean --force` ثم أعد المحاولة. |
| المنفذ مستخدم | أغلق أي نافذة تشغيل سابقة، أو أعد تشغيل اللابتوب. |

---

## ٩) أهم المزايا في النظام

- لوحة تحكم رئيسية بمؤشرات الأداء.
- سندات القبض والصرف، والقيود اليومية.
- **تصفير الصندوق**: بنقرة واحدة يُحوَّل **كامل رصيد الصندوق** إلى حساب
  مسحوبات أشرف (القيد: مدين مسحوبات أشرف / دائن الصندوق)، ويصبح رصيد الصندوق صفراً.
- استلام وتسليم الأجهزة، البرمجة الآجلة، بيع الإكسسوارات وبيع الرصيد.
- تقارير مالية وتحليلية مع **نظام طباعة احترافي مستقل** (رأس بالشعار واسم
  التقرير والفترة، جدول منظّم، مجاميع، تذييل برقم الصفحة واسم المستخدم ووقت الطباعة).
- وضع ليلي/نهاري، ودعم كامل للغة العربية (RTL).

---

تم إعداد هذا الدليل لنظام **Nasim ERP**. بالتوفيق في الاستخدام 🌿
